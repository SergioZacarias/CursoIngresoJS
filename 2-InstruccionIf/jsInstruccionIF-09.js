function mostrar() {
	let vMaximo = 10;
	let vMinimo = 1;
	let num;

	num = Math.round(Math.random() * (vMaximo - vMinimo) + vMinimo);

	alert(num);


}
//Genero el número RANDOM entre 1 y 10 Al presionar el Botón, mostrar un número Random del 1 al 10 inclusive//